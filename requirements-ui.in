# Streamlit UI dependencies (Wave 10). The runtime services do NOT need
# Streamlit, so it stays out of `requirements.in` / `requirements-dev.in`
# to keep the gateway / ingest / RAG containers slim. Install with:
#
#   pip install -r requirements-ui.txt
#
# then launch with:
#
#   streamlit run ui/streamlit_app.py
#
# UI-only dependencies. Do not include `-r requirements.in` here: the
# Streamlit container/client talks to the gateway over HTTP and does not
# need the backend service stack installed.
httpx>=0.28.0
streamlit>=1.36.0
